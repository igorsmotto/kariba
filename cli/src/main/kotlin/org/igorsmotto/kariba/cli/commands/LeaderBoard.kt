package org.igorsmotto.kariba.cli.commands

import com.github.ajalt.clikt.core.CliktCommand

class LeaderBoard : CliktCommand("Check the leaderboards") {
    override fun run() {
        return Unit
    }
}